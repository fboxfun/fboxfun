---
// Contact Popup Modal with fade animations
---

<div
  id="contact-popup"
  class="hidden fixed inset-0 bg-black/80 z-[9999] items-center justify-center p-4"
>
  <div class="bg-[#191919] rounded-2xl max-w-md w-full p-8 relative animate-fadeIn border-2 border-[#252525]">
    <!-- Close Button -->
    <button
      id="close-popup"
      class="absolute top-4 right-4 text-gray-400 hover:text-white text-2xl font-bold w-8 h-8 flex items-center justify-center"
      aria-label="Close"
    >
      ×
    </button>

    <!-- Popup Content -->
    <div class="text-center mb-8">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">Get Started</h2>
      <p class="text-gray-400">Provide your email to start opening free mystery boxes!</p>
    </div>

    <form id="contact-form" class="space-y-6">
      <!-- Email Field -->
      <div>
        <label for="popup-email" class="block text-sm font-medium mb-2">
          <span class="text-[#FF3851]">*</span> Your Email
        </label>
        <input
          type="email"
          id="popup-email"
          name="email"
          required
          class="w-full px-4 py-3 bg-transparent border-2 border-[#252525] rounded-lg focus:border-[#FF3851] focus:outline-none transition-colors"
          placeholder="Enter your email"
        />
      </div>

      <!-- Submit Button -->
      <button
        type="submit"
        class="w-full bg-[#FF3851] hover:bg-[#FF6B7A] text-white font-semibold py-3 px-6 rounded-full transition-colors text-lg"
      >
        Play for Free
      </button>
    </form>

    <p class="mt-6 text-xs text-gray-500 text-center">
      By registering, I confirm that I am at least 18 years of age and that I have reviewed and accepted the
      <a href="https://www.fortunebox.fun/terms-of-service" class="text-[#FF3851] underline">Terms of Service</a>
      and the
      <a href="https://www.fortunebox.fun/privacy-policy" class="text-[#FF3851] underline">Privacy Policy</a>.
    </p>
  </div>
</div>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-fadeIn {
    animation: fadeIn 0.35s ease-out;
  }
</style>
