---
// Contact Popup Modal with fade animations
---

<div
  id="contact-popup"
  class="hidden fixed inset-0 bg-black/80 z-[9999] items-center justify-center p-4"
>
  <div class="bg-[#191919] rounded-2xl max-w-md w-full p-8 relative animate-fadeIn border-2 border-[#252525]">
    <!-- Close Button -->
    <button
      id="close-popup"
      class="absolute top-4 right-4 text-gray-400 hover:text-white text-2xl font-bold w-8 h-8 flex items-center justify-center"
      aria-label="Close"
    >
      ×
    </button>

    <!-- Popup Content -->
    <div class="text-center mb-8">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">Get Your Bonus Now!</h2>
      <p class="text-gray-400">Sign up and start winning today</p>
    </div>

    <form id="contact-form" class="space-y-6">
      <!-- Name Field -->
      <div>
        <label for="popup-name" class="block text-sm font-medium mb-2">
          <span class="text-[#FF3851]">*</span> Your Name
        </label>
        <input
          type="text"
          id="popup-name"
          name="name"
          required
          class="w-full px-4 py-3 bg-transparent border-2 border-[#252525] rounded-lg focus:border-[#FF3851] focus:outline-none transition-colors"
          placeholder="Enter your name"
        />
      </div>

      <!-- Email Field -->
      <div>
        <label for="popup-email" class="block text-sm font-medium mb-2">
          <span class="text-[#FF3851]">*</span> Your Email
        </label>
        <input
          type="email"
          id="popup-email"
          name="email"
          required
          class="w-full px-4 py-3 bg-transparent border-2 border-[#252525] rounded-lg focus:border-[#FF3851] focus:outline-none transition-colors"
          placeholder="Enter your email"
        />
      </div>

      <!-- Phone Field -->
      <div>
        <label for="popup-phone" class="block text-sm font-medium mb-2">
          Phone (Optional)
        </label>
        <input
          type="tel"
          id="popup-phone"
          name="phone"
          class="w-full px-4 py-3 bg-transparent border-2 border-[#252525] rounded-lg focus:border-[#FF3851] focus:outline-none transition-colors"
          placeholder="Enter your phone number"
        />
      </div>

      <!-- Submit Button -->
      <button
        type="submit"
        class="w-full bg-[#FF3851] hover:bg-[#FF6B7A] text-white font-semibold py-3 px-6 rounded-full transition-colors text-lg"
      >
        Play for Free
      </button>
    </form>
  </div>
</div>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-fadeIn {
    animation: fadeIn 0.35s ease-out;
  }
</style>
